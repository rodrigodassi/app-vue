{
  "name": "intecred-api",
  "version": "0.1.0",
  "private": false,
  "description": "APP with json serve",
  "main": "src/index.js",
  "scripts": {
    "serve": "nodemon ./src/index.js --port 5000 -d 500",
    "build": "babel src --out-dir ./build --source-maps -D"
  },
  "babel": {
    "presets": [
      "module:intecred-babel"
    ]
  },
  "dependencies": {
    "json-server": "^0.16.1",
    "json-server-auth": "^2.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
